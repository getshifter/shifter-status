{
  "name": "shifter_status",
  "description": "Welcome to Shifter status, a place for current data and system performance.",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "statusfy dev",
    "build": "statusfy build",
    "start": "statusfy start",
    "generate": "statusfy generate",
    "new-incident": "statusfy new-incident"
  },
  "dependencies": {
    "statusfy": "^0.1.3"
  },
  "engines": {
    "node": ">=8.10",
    "npm": ">=5"
  }
}
